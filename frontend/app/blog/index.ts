import type { BlogPost } from "../data";

// To add a blog post:
// 1. Create a new .json file in this folder (e.g. my-post.json)
// 2. Import it below and push it into the posts array

// import myPost from "./my-post.json";

const posts: BlogPost[] = [
  // myPost as BlogPost,
];

export default posts;
